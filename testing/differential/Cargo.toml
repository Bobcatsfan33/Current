[package]
name = "current-differential"
description = "The differential harness: seeded scenarios, engine vs oracle, every sealed epoch."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[dependencies]
current-zset.workspace = true
current-plan.workspace = true
current-ops.workspace = true
current-circuit.workspace = true
current-sql.workspace = true
current-memo.workspace = true
current-batch.workspace = true
current-log.workspace = true
current-state.workspace = true
current-oracle.workspace = true
rand_core.workspace = true
rand_chacha.workspace = true
thiserror.workspace = true

[dev-dependencies]
# The evidence test checks that the ledger's values match the constants in the code, and one of those
# constants lives in the soak harness. A dev-dependency cycle — `current-soak` depends on this crate —
# which cargo permits precisely because a test's dependencies are not part of the library's graph.
current-soak.workspace = true

[lints]
workspace = true
